import ListAppointmentComponent from "../ListAppointmentComponent";

function ApptDB() {
    return (
        <div>
            <h1>Appointments Database</h1>
            <ListAppointmentComponent/>
        </div>
    );
}

export default ApptDB;
